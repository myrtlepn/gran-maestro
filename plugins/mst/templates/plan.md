# Plan: PLN-{NNN}

**생성일**: {created_at}
**주제**: {topic}

---

## 요청 (Refined)

{원본 요청에서 정제된 구체적 요구사항}

## 범위 예산 (Appetite)

{예상 소요: 1일 이내 / 1주 이내 / 2주+ 중 선택, 또는 자유 기술. 빈 값 허용.}

## 제외 범위 (No-go Scope)

{이번 plan/REQ에서 명시적으로 다루지 않는 항목. 빈 값 허용.}

## 결정 사항

| 항목 | 결정 | 근거 |
|------|------|------|
| {항목} | {결정} | {사용자 답변 또는 합의} |

## 범위

**포함**: {이번에 구현할 내용}

**제외**: {명시적으로 제외된 내용}

## 제약 & 가정

- {기술적 제약, 환경 제약}
- {사용자 답변 기반 가정}

## 테스트 전략

> 이 섹션은 테스트 방법론 적용 시에만 작성합니다. "적용 안 함" 선택 시 이 섹션은 생략됩니다.

- **적용 여부**: {적용 / 적용 안 함}
- **목표 커버리지**: {N% 또는 미설정}
- **비고**: {추가 테스트 관련 메모. 빈 값 허용.}

## 참고 컨텍스트

{추가로 PM이 알아야 할 배경 정보}

## 연관 컨텍스트 (해당 시에만)

> 상세 내용은 아래 파일을 직접 참조하세요. 내용을 이 파일에 복사하지 않습니다.

| 유형 | ID | 파일 경로 |
|------|----|-----------|
| 디버그 조사 | DBG-NNN | `.gran-maestro/debug/DBG-NNN/debug-report.md` |
| 캡처 | CAP-NNN | `.gran-maestro/captures/CAP-NNN/capture.json` |

## 디자인 시안 (해당 시에만)

> Stitch로 생성된 디자인 시안 정보입니다. `/mst:plan`의 Step 4에서 "스티치로 디자인 시안 보기" 선택 시 자동으로 채워집니다.

- Stitch 프로젝트: {프로젝트 URL 또는 N/A}
- 생성 화면:
  - {화면명}: {화면 URL 또는 ID}
    - 구현 코드: `.gran-maestro/designs/DES-NNN/screen-NNN.html` (또는 N/A)

## 캡처 참조 (해당 시에만)

> 이 섹션은 캡처 데이터가 포함된 경우에만 작성합니다.

| CAP ID | 페이지 | 요소 | 메모 |
|--------|--------|------|------|
| {CAP-NNN} | {URL 발췌} | {selector} | {memo} |

스크린샷: `.gran-maestro/captures/{CAP-NNN}/screenshot.webp`

## 분리 실행 (해당 시에만)

> 이 plan은 독립적인 복수 REQ로 실행하는 것을 권장합니다.
> 각 REQ는 하나의 책임(기능/레이어/도메인)만 가져야 합니다.

| 순서 | 작업 (책임 단위) | 분리 이유 | 병렬 가능 |
|------|-----------------|-----------|-----------|
| ① | {첫 번째 책임} | {분리 이유} | {yes/no} |
| ② | {두 번째 책임} | {분리 이유} | {yes/no} |

## Loop 종료 조건

> 이 plan의 review 반복 루프에 적용될 추가 종료 조건입니다.
> 조건 미설정 시 기존 동작(AC 통과 + max_iterations)으로 작동합니다.

- {조건 1}
- {조건 2}

## 인수 기준 초안

> 이 plan이 완료됐다는 것은 무엇을 의미하는가?
> 구현 방법이 아닌 관찰 가능한 결과/동작을 기준으로 작성합니다.
> mst:request가 이 섹션을 읽어 상세 AC(Given-When-Then)로 변환합니다.
> 저장 시 각 항목은 `PAC-N` ID가 자동 부여되며 `plans/PLN-NNN/plan.ids.json`에 `[{ "id", "text", "grade", "tags" }]` 형식으로 기록됩니다.
> 항목 앞에 `[SHOULD]` 태그를 붙이면 grade가 SHOULD로 기록되고, 미기입 시 MUST로 기록됩니다.
> 각 항목에는 `[TIER-A]` 또는 `[TIER-B]` 태그를 의무적으로 포함합니다.
> 분류 기준: `TIER-A`는 비즈니스 규칙·상태 전이·데이터 변환, `TIER-B`는 UI·문구·설정·저위험 변경입니다.
> Tier 태그가 누락되면 기본값 `TIER-B`로 간주됩니다.

이 plan의 구현이 완료됐다는 것은:
- {관찰 가능한 완료 조건 1}
- {관찰 가능한 완료 조건 2}
- {PM이 직접 확인 가능한 동작 3}
